#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a, b, c;

    cout << "Ingrese cateto a: ";
    cin >> a;

    cout << "Ingrese cateto b: ";
    cin >> b;

    c = sqrt(a * a + b * b);

    cout << "La hipotenusa c : " << c << endl;

    return 0;
}
